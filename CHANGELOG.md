# Changelog

## 1.15.2 - 2026-09-13

- 修复 sing-box 订阅生成 VLESS 出站时用原始用户 UUID 覆盖节点身份的问题，保留前置直出及各个落地独立的路由编号，避免选中落地后仍从前置默认出口出去。
- 与 mihomo 使用同一份节点身份；普通 VLESS 节点保持原有身份，未提供节点身份时继续回退到用户 UUID。
- 补充最终 JSON/YAML 订阅对照，覆盖 VLESS/HY2 入口、双核心组合、两种落地协议、重复生成、停用恢复、身份轮换和拓扑失效。
- 本地 PHP `8.4.21` 完整回归 185 项测试、2546 个断言通过，其中中转回归 38 项测试、895 个断言通过。
- 本次仅修改面板订阅生成，没有数据库迁移、Node 通信或核心依赖变化；发布来源和验证记录见 [兼容矩阵](YZ_COMPATIBILITY.md)。

## 1.15.1 - 2026-09-13

- 插件上传独立使用 5 分钟请求超时，普通管理端请求继续使用 30 秒；浏览器超时会提示先刷新插件列表确认结果，再决定是否重试。
- 补齐管理端请求错误处理，保留服务端消息、表单校验详情和客户端错误原因；断网、请求取消或中断、HTTP 408 / 413 / 5xx 以及 HTML 错误页均提供明确提示。
- 插件上传由页面统一显示一次失败原因，失败后恢复上传状态；不自动重复发送上传请求。
- 构建补丁支持从旧版 64 MiB 上传补丁升级，保持重复执行、资源摘要和入口引用一致；新增超时、错误展示及失败后重新上传的回归覆盖。
- 本地完整管理端 72 项测试通过，其中插件上传相关 16 项；PHP 完整回归 183 项测试、2420 个断言通过。
- 本次没有数据库迁移或 Node 通信变化，配置限制和错误排查见 [插件上传说明](docs/plugin-upload.md)。
- `v1.15.1` 与双架构 GHCR 镜像已发布，`latest` 已更新；正式 PHP 8.2 回归、管理端测试和两个架构的实际后台文件均已核验，来源、digest 与回滚记录见 [兼容矩阵](YZ_COMPATIBILITY.md)。

## 1.15.0 - 2026-09-13

- 节点列表在“类型”旁新增“内核”筛选，支持 sing-box 和 Xray 多选，并可与类型、服务器和权限组组合筛选。
- 内核分类与后端一致：兼容 `singbox`、`sing-box` 和大小写；历史空值及未知值按 Xray 处理，选项数量随其他筛选条件更新。
- 内核筛选支持清空、重置和列表刷新，桌面与窄屏使用相同控件，排序模式保持原有布局。
- 合并此前未发布的 `1.14.1` 权限组选项修复。
- 节点批量添加权限组只显示至少一个所选节点尚未拥有的组，所有所选节点都已拥有的组不再显示。
- 批量移除只显示至少一个所选节点已经拥有的权限组；多选、重新选择和列表刷新时按当前节点数据重新计算。
- 兼容数字与字符串形式的权限组编号，没有可操作项时显示禁用提示；沿用已有增量更新接口，无数据库迁移或 Node 通信变更。
- 构建补丁支持更新已有的批量权限组菜单，并同步更新管理端资源摘要与入口引用。
- 新增 7 项内核筛选测试，连同已有的 10 项权限组菜单测试接入发布检查；正式 PHP 8.2 完整回归 183 项、2420 个断言及管理端 62 项测试通过。
- `v1.15.0` 与双架构 GHCR 镜像已发布，`latest` 已更新；三个标签、来源提交及两个架构的实际管理端文件已核验，来源与回滚记录见 [兼容矩阵](YZ_COMPATIBILITY.md)。

## 1.14.0 - 2026-09-13

- 节点管理列表将“显隐”改为“开关”，按当前节点的启用状态控制运行，通过现有接口单独开启或关闭绑定服务器上的节点。
- 请求期间锁定当前开关，保存后刷新列表；请求失败和内部端口冲突时保留原状态并提示原因，独立部署节点显示不可操作。
- 增加单节点启停、重复调用、同服务器其他节点保留、共享通信连接和空服务器重新开启节点的回归测试。
- 构建阶段注入运行开关并更新资源摘要，支持中、英、俄文案；没有数据库迁移或 Node 通信协议变更。使用方式见 [节点运行开关](docs/node-runtime-switch.md)。

## 1.13.5 - 2026-09-11

- 新建或修改节点时，按当前绑定服务器、内部端口和 TCP/UDP 监听范围检查重复；TCP 与 UDP 可以使用相同端口号，客户端连接端口不参与重复检查。
- 管理端填写内部端口后自动提示冲突节点，保存时重新校验；切换服务器、协议传输方式、内核、重新启用和批量绑定同样受检查，批量操作遇到冲突全部回滚。
- 复制继续保留原连接端口、内部端口、绑定服务器和启用状态；已有重复配置只修改名称等无关字段仍可保存，不自动改写或停用已有节点。
- 识别 Shadowsocks、Naive 的双传输监听、SOCKS 的内核差异及 KCP、QUIC、Hysteria 等 UDP 传输。隐藏或停用节点仍保留配置中的内部端口。
- 统一校验内部端口为 `1–65535` 的整数，保存时将表示取消绑定的机器编号 `0` 转为 `null`。没有数据库迁移或 Node 通信字段变更，规则与使用说明见 [内部端口检查](docs/node-port-validation.md)。

## 1.13.4 - 2026-09-10

- 移除套餐编辑页基础价格的默认周期折扣，批量填价改为“基础价格 × 周期月数”；基础价格为 10 时，季付为 30、半年付为 60、年付为 120。
- 保留各周期单独定价和两位小数；已有套餐价格保持原值，流量包和重置包继续按基础价格填入。
- 镜像构建时应用套餐价格补丁并更新管理端资源名称和入口引用，避免浏览器沿用旧的折扣计算代码。

## 1.13.3 - 2026-09-10

- 同步 `cedar2025/Xboard` 从 `8ecb762` 到 `4f48e61` 的 6 个提交，保留 YZboard 的中转、设备状态等扩展，并统一发布默认内核和插件上传修复。
- 订单创建、支付和取消在事务内重新读取并锁定当前状态，避免重复扣除余额、开通订阅或退款；礼品卡兑换重新校验当前状态，邀请人奖励发放失败时回滚整次兑换。
- 邀请佣金按余额抵扣后的实付金额计算，拒绝负金额订单结算；修正支付宝当面付请求时间的分钟格式。
- 修复通用 VLESS 链接名称中的空格编码，补充 Shadowrocket 的 Hysteria2 上下行带宽参数；设备 IP 去重列表修复与本 fork 已有实现一致。
- 保留 YZboard 测试隔离配置，新增及同步 19 项订单、礼品卡和订阅回归用例；完整 PHP 回归 142 项、2042 个断言及管理端 11 项测试通过。来源、配套关系及发布状态见 [兼容矩阵](YZ_COMPATIBILITY.md)。

- 插件 ZIP 上传上限由 10 MiB 提高到 64 MiB，支持包含本地 IPv4、IPv6 数据库的安装包。
- Docker 中 PHP 的单文件上限设为 64 MiB、整次请求上限设为 80 MiB；Swoole 请求上限同步为 80 MiB，避免表单边界占用空间导致临界大小文件被提前拒绝。
- 管理端在发送前拦截超限插件包，并为 HTTP 413 提供明确提示；镜像构建时同步更新管理端资源名称和引用。
- 验证 64 MiB 边界、超出一字节、错误文件类型、413 响应及构建补丁幂等性；部署配置和检查步骤见 [插件上传说明](docs/plugin-upload.md)。

- 新建节点默认使用 sing-box，VLESS 仍默认使用 Xray；管理端表单和保存接口使用相同规则，手动选择优先。
- 编辑、复制和配置下发保留已有节点的内核；历史空值继续按 Xray 处理，不修改数据库中的已有记录。
- 机器安装命令取消强制指定 Xray，配合正式版 Node `v1.13.1` 为新绑定写入默认值，重复安装保留原内核。
- 增加新建、重复保存、复制、机器发现与配置下发测试，以及管理端补丁生成、协议切换和幂等验证。
- 发布工作流在构建镜像前执行 PHP 8.2 全量测试和管理端补丁测试；正式版本、固定来源和双架构镜像保持一致。

## 1.13.0 - 2026-09-09

- Hysteria2 连接端口支持单端口列表、连续范围及混合写法，并校验端口边界、空项、倒序范围和至少 5 秒的固定跳跃间隔。
- 节点配置通过 `port_hopping` 下发跳跃集合，`server_port` 保持实际单端口监听；支持普通 HY2 节点和中转入口。
- 订阅选择只落在有效端口集合内，统一规整空格、重复和重叠范围；修复 sing-box 混合列表被输出成单个非法范围的问题。
- 配合 Node `v1.13-yz.24` 自动放行及回收防火墙规则，无数据库变更；配置和升级顺序见 [防火墙与端口跳跃说明](docs/firewall-port-hopping.md)。

## 1.12.1 - 2026-09-08

- 修复 Mihomo 订阅遗漏 XHTTP 请求头、填充、会话参数、XMUX 和上下行分离配置的问题，遵循 Xray 的 `extra` 优先级并转换为 Mihomo 字段。
- 下载链路独立转换地址、端口、TLS、Reality 和 ECH 公共参数，避免继承上行的认证设置；不支持或无效的 XHTTP 组合从订阅中过滤。
- 按 Xray 语义处理 XHTTP 零值、倒序范围、填充和上传默认字段；上下行 XMUX 分别转换，无 TLS 下载显式使用 HTTP/1.1。
- ECH 版本判断覆盖布尔开关，补齐 HY1、TUIC 的 ECH 下发；基础 ECH、DNS 查询域名以及不同 XHTTP 能力使用各自的内核最低版本，不再用应用外壳版本判断这些能力。
- 订阅增加 `mihomo` 标识识别，保留未知内核版本的节点；修复 VLESS、VMess 旧 HTTP/2 别名被当成 TCP 输出的问题。
- 补齐 WebSocket / HTTPUpgrade 请求头、TCP HTTP 伪装请求方法、gRPC User-Agent 和可表示的 authority；无法独立表示 TLS 服务名与 authority 的组合从订阅中过滤。
- 修复 Mieru 同时输出单端口和范围的问题，启用 UDP 并补齐流量模式；补 HTTP TLS 服务名及 Reality 默认指纹，过滤 HTTP/SOCKS5 无法表示的 TLS 参数。
- 修复 Shadowsocks 插件布尔开关、默认选项、ShadowTLS 扩展和 Restls 必需字段，避免固定输出无效 Restls 脚本。
- sing-box 多路复用按实际入口内核生成，修复中转节点误用落地内核的问题；补齐 sing-box HY1 入站需要的 ALPN。
- AnyTLS、Mieru、VLESS Encryption 和 Mieru 流量模式分别按实际内核版本过滤。
- 配套 Node 和核心依赖沿用 `1.12.0`，本次只修改面板订阅生成与中转对象的入口内核投影。完整协议范围与验证记录见 [Mihomo 订阅兼容说明](docs/mihomo-subscription.md)。

## 1.12.0 - 2026-09-08

- HY2 前置入口支持 ECH，Xray 与 sing-box 入口均可挂接现有 VLESS／Shadowsocks 落地；入口需要提供 ECH 服务端密钥，保留 HY1、无效混淆和内部协议组合的限制。
- 补齐 Mihomo HY2 订阅的 `ech-opts`，保留 sing-box 的 `tls.ech`；逻辑节点继续继承入口参数并保留各自的路由认证值，订阅只包含 ECH 公共配置。
- HY2 ECH 增加独立的客户端版本判断，正确处理布尔开关；识别为 `meta` 且内核低于 `1.19.9` 时不下发此类节点。没有版本信息时保留节点，不用应用外壳版本推断其内核版本，不改变其他协议的版本规则。
- 配套 Node 版本为 `v1.13-yz.22`，修复 Xray HY2 入站遗漏 ECH 密钥的问题。固定核心依赖保持不变；ECH 握手、中转、混淆、密钥轮换、计数验证及正式发布引用见兼容矩阵。

## 1.11.0 - 2026-09-08

- 中转入口与落地新增 sing-box 支持，可以与 Xray 混用；客户端仍通过同一 VLESS/Hysteria2 入口和端口选择不同落地。
- 前置入口候选由后端按节点实际能力判断；保存落地或切换入口内核时，同时校验两端是否支持内部协议，旧数据中的无效组合继续从订阅和配置中排除。
- 任一端使用 sing-box 时，VLESS 内部链路支持 RAW/TCP、WebSocket、gRPC、HTTPUpgrade；拒绝 VLESS Encryption、TCP 头部伪装及无效 Vision 组合。公网 VLESS 内部链路需使用 TLS/Reality。
- 管理端补丁支持从原始资源、旧 VLESS 补丁和 HY2 补丁升级，重复应用保持幂等。
- 配套 Node 为 `v1.13-yz.21`，使用 Xray `v26.7.11-yz.6`、sing-box `v1.14.0-yz.2` 及固定 SS2022 关闭补丁，修复 VLESS 出站上传漏计、原三类并发问题及发布前发现的 HY2 会话关闭竞争；报告结构、入口倍率及用户只扣费一次的规则保持兼容。固定版本与验证记录见兼容矩阵。

## 1.10.0 - 2026-09-07

- 前置入口支持原生 Hysteria2：使用 Xray 的 HY2 节点可挂接现有 Shadowsocks/VLESS 落地，沿用稳定路由编号和中转配置结构。
- HY2 订阅在认证 UUID 中写入逻辑节点编号，继承入口的地址、端口、TLS、带宽和混淆参数；修复 sing-box 订阅重新使用原始 UUID、丢失路由编号的问题。
- 保存和拓扑投影校验入口协议及 Xray 内核，拒绝 HY1、未支持的 HY2 ECH 和无效混淆设置；普通 HY2 节点的认证保持原样。
- 列表切换内核与编辑表单共用中转校验，编辑时省略可选字段按已保存值校验；旧数据中的无效落地不再进入订阅和入口配置。
- 管理端前置入口候选增加 HY2，并支持从既有 VLESS 候选补丁增量升级；重复执行保持幂等。
- 用户套餐流量继续只在入口结算，落地总流量和用户-落地明细沿用原有报告字段。配套 Node 源码目标版本为 `v1.13-yz.20`。

## 1.9.0 - 2026-09-03

- 节点报告新增 `relay_user_traffic`，接收中转入口按用户和逻辑落地节点拆分的原始上下行流量。
- 报告批次保存新字段并在结算事务提交后触发 `traffic.relay_user.processed` Hook；不改变原有用户套餐扣除和落地节点总量统计。
- 非当前入口的中转子节点、负数和错误结构会在接收层丢弃，重复报告继续按 `report_id` 幂等处理。

## 1.8.3 - 2026-09-02

- 修复管理端节点编辑补丁多插入一组闭合括号的问题。该问题会让 Chrome 在加载新版 bundle 时报告 `Unexpected token ']'` 并再次显示白屏；补丁现在按浏览器兼容语法生成。

## 1.8.2 - 2026-09-02

- 修复节点编辑页点击编辑时报 `getFieldState` 空上下文错误的问题。内核下拉位于弹窗标题区域，改为直接读写当前表单实例，不再依赖 `FormProvider`；协议选择左侧布局、默认 Xray 和下拉保存行为保持不变。

## 1.8.1 - 2026-09-02

- 修复管理端节点内核选择器的构建期注入缺少闭合调用的问题，避免管理端 JavaScript 解析失败导致页面白屏。

## 1.8.0 - 2026-09-02

- 节点支持独立选择运行内核：每个节点可使用 Xray 或 sing-box，同一台机器可以混合运行两种内核；未选择的历史节点默认按 Xray 处理。
- 机器模式按节点实际内核创建服务实例，只有 Xray 节点时不会额外启动空的 sing-box 实例；节点切换内核时只重启对应节点。
- 管理端节点表单在协议选择左侧增加内核下拉，节点内核变更会通过机器节点同步即时生效。

## 1.7.0 - 2026-08-31

- 节点流量报告改为先写入 `v2_node_report_batch`，再由可重试任务在同一数据库事务中完成用户流量、用户日统计、节点日统计和中转流量累计；相同 `server_id + report_id` 只结算一次，队列派发或执行失败不再让 Node 误删尚未结算的批次。
- 新增 `node:retry-reports` 每分钟补偿未完成报告，已完成批次保留 7 天用于 HTTP 重试去重；升级后必须先执行数据库迁移。
- 节点每次合法报告都会刷新 `last_check_at` 和 `last_push_at`。在线人数改用 Node 的 `online` 快照，空快照会写入 0 并清理该节点已离线用户的连接缓存，不再用“本次有流量的用户数”代替在线人数。
- 设备状态改为按节点整体替换的权威快照，支持空快照、稳定在线 TTL 续期和旧 Redis 数据的一次性索引迁移；节点设备查询不再长期使用全量 `KEYS` 扫描。
- 批量封禁用户和套餐强制更新会在事务完成后按受影响的新旧权限组派发全量用户同步，避免绕过用户观察器后等待定时任务。
- WebSocket 旧连接关闭时只清理仍由该连接持有的节点状态，避免重连竞态擦除新连接的在线标记和设备快照。

## 1.5.0 - 2026-08-20

- 节点管理批量操作新增按权限组添加、移除功能：只增量修改目标权限组，保留节点已有的其它权限组，重复操作不会产生重复 ID 或误删其它组。
- Shadowsocks 新建节点默认及首选加密算法改为 SS2022 128 位 AES-GCM（`2022-blake3-aes-128-gcm`）；已有节点配置不变。

## 1.4.0 - 2026-08-09

- 中转落地协议在保留 Shadowsocks 的基础上新增 VLESS。入口端生成独立 VLESS 出站，落地端生成只含面板派生内部 UUID 的 VLESS 入站，不下发面板用户。
- VLESS 内部链路支持 RAW/TCP、WebSocket、gRPC、XHTTP、HTTPUpgrade、mKCP 和 Hysteria，并按当前 YZ-Xray-core 限制 TLS/Reality 组合；H2/HTTP、mKCP 旧 header/seed、无效 Flow、ECH 和 `allowInsecure` 会在保存时拒绝，已被落地引用的入口也不能改成无效配置。
- 支持把节点页面已有的 VLESS Encryption `encryption`/`decryption` 对用于入口到落地的内部链路。客户端公有项只发给入口，decryption、Reality 私钥和证书配置只留在落地。
- 公网 VLESS 内部链路在不使用 TLS/Reality 时强制启用 VLESS Encryption；内网链路仍允许显式 `none`。
- 前置入口不再限定为 VLESS Reality。只要入口本身是有效的 VLESS + Xray 组合，WS、gRPC、XHTTP、HTTPUpgrade、mKCP、Hysteria 等有效传输都可以承载中转入口流量。
- 管理端构建期补丁同步修正 VLESS 下拉：移除 H2 和过时 Flow，增加 Hysteria，并在切换传输后自动纠正已知无效的 Reality/Hysteria 组合。VLESS Encryption 的 `decryption` 输入框新增与 Reality 私钥一致的钥匙按钮，复用浏览器 X25519 生成器并成对填入两项。补丁继续使用锚点失败即中止构建和内容 hash 重命名。
- 中转配置、订阅私密字段隔离、前置入口与内部链路各 16 组有效传输组合及 VLESS Encryption 增加面板与 Node 回归测试；Node 生成的入口、内部出站和落地配置均由固定 YZ-Xray-core 解析验证。

## 1.3.1 - 2026-07-27

- 节点列表「前置入口」列的徽标改用权限组列的样式：淡底、浅描边、悬停加深。原先跟的是倍率列的实心底色，单值列用实心块在整行里显得过重。
- 不带权限组那三个 flex 类。权限组的徽标装在 flex 容器里，而这一列的徽标是单元格直接子元素，加上去会让徽标从 inline-flex 变成块级并撑满整列宽度。

## 1.3.0 - 2026-07-27

- 「中转入口」更名为「前置入口」。表单标签、占位符和保存节点时的校验错误信息统一改用新说法，数据库列名 `relay_entry_id` 和节点端接口不变，升级无需迁移。
- 节点列表表头新增「前置入口」列，位于「部署方式」与「地址」之间，显示该节点走哪个入口，未设置显示 `--`。该列在拖拽排序模式下与其他数据列一同隐藏。
- 管理端节点列表接口 `getNodes` 增加 `relay_entry_name` 字段。名称在服务端用同一份全量节点列表解析，不额外查库；入口节点被列表搜索或筛选排除时，逻辑节点这一列仍然显示得出来。
- 构建阶段补丁 `.docker/patch-admin-relay.php` 同步注入上述列和列显隐登记，锚点匹配不到时镜像构建直接失败。产物只带有一半补丁标记时同样失败，避免把表单字段重复注入第二遍。
- 订正 `ServerRelayService` 的类注释：中转判定依据早在 1.2.0 就从父级节点改为前置入口，注释一直沿用旧说法。

## 1.2.1 - 2026-07-27

- 修复管理端补丁不触发浏览器缓存失效的问题。构建产物的文件名 hash 是打包时算出来的，补丁只改内容不改文件名，浏览器会继续使用缓存里的旧产物，导致「中转入口」下拉在界面上不出现。
- 补丁改为按打完补丁后的内容重新命名入口文件，并同步更新 `manifest.json` 与 `index.html` 中的引用；找不到任何引用时构建失败。命名由内容推导，重复构建产出相同文件名。

## 1.2.0 - 2026-07-27

- 中转关系改用新增的 `relay_entry_id` 字段，不再借用 `parent_id`。`parent_id` 的行为完整回退到上游语义：共享运行状态缓存、共享 SS2022 服务端密钥、掉线告警跳过子节点、流量排行显示父节点名称，与 `cedar2025/Xboard` 逐行一致。
- 两个字段互不干扰。一个节点可以同时设置父级节点和中转入口；中转判定只看 `relay_entry_id`，`relay_entry_id` 为 0 或 NULL 都表示不使用中转。
- 升级后存量节点全部不受影响：新列对既有节点为空，不会有任何节点被识别成中转逻辑节点。
- 管理端节点编辑表单新增独立的「中转入口」下拉，候选为没有中转入口的 VLESS 节点。原有的「父级节点」下拉保持不变（它按同协议过滤，无法表达跨协议的中转关系，这也是拆成两个字段的原因）。
- 该下拉由构建阶段的定点补丁 `.docker/patch-admin-relay.php` 注入到管理端构建产物；锚点匹配不到时镜像构建直接失败，不会静默产出缺少该字段的管理端。补丁可重复执行，且不改动原有字段。
- 保存节点时的中转校验改为针对中转入口，错误信息相应调整（自引用、多层中转、入口套入口、入口非 VLESS、不支持的中转加密算法）。

## 1.1.0 - 2026-07-26

- 新增中转节点能力：一个 VLESS 入口节点可以带多个中转逻辑节点，订阅中显示为多个独立的普通节点，共用入口地址、端口和全部 Reality 参数，出口分别落在各自的落地服务器。
- 重新定义节点的“父级节点”字段。父级节点为空时行为不变；父级节点不为空时当前节点是中转逻辑节点，父级节点是客户端实际连接的真实入口。第一版只支持“一个真实入口加一层落地”，中转协议只支持 Shadowsocks。
- 节点表增加 `vless_route`，由面板自动分配 1-65535 的路由编号并写入订阅 UUID 的第 7、8 字节。编号创建后保持稳定，分配游标只增不减，删除节点后不会立即复用。
- 保存节点时校验中转拓扑，拒绝自引用、多层中转、入口套入口、非 VLESS 父级和不支持的中转加密算法。
- 节点配置接口增加 `relay` 段：入口节点下发全部逻辑节点的内部出站参数和路由编号映射，落地节点只下发自己那条内部入站的监听参数。内部认证信息由应用密钥派生，不落库、不进入订阅。
- 落地节点不再下发面板用户，避免在落地端重复统计用户流量。
- 节点上报接口接受 `relay_traffic`，按出站标签或逻辑节点 ID 记入落地节点的节点流量，不参与用户套餐扣费，也不套用倍率。
- 中转逻辑节点强制继承入口的基础倍率和动态时段倍率，自身填写的倍率不参与用户扣费。
- 中转逻辑节点使用自身的心跳、在线数和负载状态，掉线告警不再被跳过；旧语义的子节点保持原行为。

## 1.0.6 - 2026-07-26

- 服务器管理的机器安装命令改用 YZboard-Node 最新正式 GitHub Release 中的安装器，并显式选择 Xray 内核。
- 在线设备 IP 去重后重新生成连续数组，避免 PHP 将带空洞的数组编码为 JSON 对象。
- YZboard Docker 正式版本继续同时发布不可变镜像标签、版本别名和 `latest`，生产 Compose 可固定使用 `latest` 拉取更新。

## 1.0.5 - 2026-07-26

- 订阅响应头中的上传、下载、总量和到期时间统一输出为非负整数，兼容仅接受整数字节数的客户端。
- 该格式化仅发生在订阅响应阶段，不修改数据库流量、节点上报、套餐扣量或限额判断。

## 1.0.4 - 2026-07-26

- YZboard Docker 发布改为从固定 Tag 或完整 commit 构建，不再在镜像内跟随移动分支。
- GitHub Actions 发布 `linux/amd64`、`linux/arm64` 到 `ghcr.io/p0me1oo/yzboard`，并生成“面板版本 + 短 commit”的不可变镜像标签。
- 工作流保留可选的面板版本别名和 `latest`，生产部署要求使用不可变标签并校验多架构 manifest。
- 镜像内版本保持 `config/app.php` 的语义版本，不再在构建期间临时改写成日期和短 SHA。

## 1.0.3 - 2026-07-25

- 服务器管理生成的机器安装命令固定使用 `P0me1oo/YZboard-Node` 的 `v1.13-yz.2` 安装器和 Release。
- 安装命令显式传入 Node 版本，避免后续 `latest` 变化导致面板与 Node 兼容关系漂移。
- Node 安装和升级资产增加 `SHA256SUMS` 完整性校验。

## 1.0.2 - 2026-07-25

- 增加 `xray-v26.7.11-yz.1` YZ-Xray-core fork 兼容标识。
- 配套 Node 使用 `v1.13-yz.1`，延续上游 `v1.13` 版本线；旧的 `v0.1.0-yz.1` 不作为部署版本。
- Node 重试同一报告批次时，V2 节点流量上报保持幂等，避免重复累计。
- 保持心跳、在线连接状态、指标和 `[upload, download]` 流量方向不变。

## 1.0.1 - 2026-07-06

- Added `node:sync-users` to periodically reconcile online node user lists with
  the panel's current eligibility rules.
- Scheduled node user reconciliation every five minutes so naturally expired
  users are removed from node runtime authentication tables without requiring a
  node restart.
